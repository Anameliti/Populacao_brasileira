import matplotlib.pyplot as plt #primeirográfico

dados = open('populacao_brasileira.csv').readlines()

x = []
y = []

for i in range(len(dados)):
    if o != 0:
        linha = dados[i].split(";")
        x.append(int(linha[0]))
        y.append(int(linha[1]))


plt.plot(x,y)
plt.show()
